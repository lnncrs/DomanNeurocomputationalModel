name: webots
channels:
  - conda-forge
dependencies:
  - python=3.13
  - pip
  - pip:
      # Instala o projeto e reutiliza o extra definido no pyproject.toml.
      - "-e .[analysis]"
      - "pytest>=9.1.1"
